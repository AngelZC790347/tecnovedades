<header>
  <div class="imgContainer">
    <img
      src="/cropped-cropped-logo300cabecera.webp"
      height="100"
      width="300"
      class="shadow-slate-950"
      alt="tecnovedades-imagotipo"
    />    
  </div>
  <nav>
    <ul>
      <li>
        <a href="./">inicio</a>
      </li>
      <li>
        <a href="./portafolio">portafolio</a>
      </li>
      <li>
        <a href="./#eligetuweb">elige tu web</a>
      </li>
      <li>
        <a href="./nosotros">nosotros</a>
      </li>
      <li>
        <a href="./contacto">contacto</a>
      </li>
    </ul>
  </nav>
  <nav id="nav-r">
    <ul>
      <li><i aria-hidden="true" class="fab fa-facebook"></i></li>
      <li></li>
    </ul>
  </nav>
</header>
<script>
  let el = Array.from(document.querySelectorAll("a")).find(
    (e) => e.href == window.location.href
  );
  if (el) {
    el.className = "active";
  }
</script>
<style>
  li {
    list-style-type: none;
  }
  .imgContainer {
    width: 30%;
    display: flex;
    justify-content: end;
  }
  img {
    margin-left: 8%;
    transform: scale(0.8);
  }
  header {    
    padding: 5px 15%;
    border-top: solid 9px #0066ff;
    box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.5);
    position: sticky;
    z-index: 9999;
    justify-content: center;
    background-color: var(--fg-color-1);
    top: 0px;
    display: flex;
    ul {
      margin: 0;
      height: 100%;
      display: flex;
      align-items: center;
    }
    @media (max-width: 724px) {      
      flex-direction: column;
      position: relative;
      align-items: center;
      li {
        width: 100%;
      }
    }
    #nav-r {
      width: 20%;
    }
    nav:not(#nav-r) {
      width: 50%;
      ul {
        flex-wrap: wrap;
        margin-right: auto;
        margin-left: auto;
        li {
          transform: translateY(2px);
          display: flex;
          line-height: normal;
          transition-timing-function: ease-in-out;
          transition-duration: 0.3s;
          border-radius: 10px;
          box-sizing: border-box;
          color: var(--fg-color);
          a {
            display: block;
            padding: 15px 20px;
            letter-spacing: 0.2;
            font-size: var(--size-medium);
            font-family: var(--primary-font-condessed);
            font-weight: 500;
            font-stretch: 20%;
            text-transform: uppercase;
            text-decoration: none;
            color: inherit;
            word-spacing: 0px;
          }
          text-align: center;
        }
        .active {
          background-color: var(--fg-color-3);
          border-radius: 10px;
          color: var(--fg-color-1);
        }
        li:hover {
          background-color: var(--fg-color-3);
          color: var(--fg-color-1);
        }
      }
    }
  }
</style>
